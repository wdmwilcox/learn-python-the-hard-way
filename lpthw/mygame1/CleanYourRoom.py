# imports
from sys import exit

import Person
import Room
import Item

# classes
class Kitchen(Room):

	def __init__(self, name, state):
		name = self.name
		state = self.state
		pass
		
	items = ["sink", "trash can", "oven"]


class BedRoom(Room):

	def __init__(self, name, state):
		name = self.name
		state = self.state
		pass
		
	items = ["bed", "dresser", "desk"]


class LivingRoom(Room):

	def __init__(self, name, state):
		name = self.name
		state = self.state
		pass
		
	items = ["couch", "chair", "coffee table"]
	responses = {

class RecRoom(Room):

	def __init__(self, name, state):
		name = self.name
		state = self.state
		pass
		
	items = ["TV", "arcade game", "pool table"]


class Player(Person):

	def __init__(self, name, score)
		name = self.name
		score = self.score
		
	def assess_room(room_name):
		pass
	
	def sort_item(item, state):
		pass


class Engine(object):

	rooms = ['kitchen', 'bedroom', 'living room', 'rec room']
	sorted_rooms = []
		
	def play(self, first_room):
		current_room = self.first_room
				
		while len(rooms) > 0:
			
		

	

# main